# chem-properties-excel v3.0.0

`v3.0.0` 将易制毒、易制爆法定名录纳入本地离线查询，并把 Excel 生成实现与 skill 定义统一到同一份 26 列数据契约。

## 主要更新

- 新增易制毒化学品参考库：38 个法定编号，规范化为 45 条检索记录。
- 新增易制爆危险化学品参考库：74 个法定编号、91 个原表物质或条件行，规范化为 95 条检索记录。
- 新增统一监管索引，支持按 CAS、规范中文名和别名查询 140 条记录。
- Excel 主表末尾新增 `是否易制毒`、`是否易制爆` 两列，主表扩为 26 列。
- 化学品清单扩为 10 列，介质-设备对照表扩为 11 列。
- 条件性易制爆条目在浓度或形态不足时输出 `需核实浓度/形态`，不会误判为“否”。
- 缺少监管索引时停止生成，防止产生监管目录假阴性。

## 正确性修复

- 删除丙烯腈与 2-氯丙烯腈的错误归并。
- 明确区分本地 GBZ 2.1-2007 数据与需另行核实的 GBZ 2.1-2019 数据。
- 修复 GB/T 42594 JSON 数组访问说明。
- GB/T 42594 本地参考未命中时提示核对原文，不再直接断言“未列入”。
- 氧化剂、遇水反应品和强酸不再自动归为“非可燃”。
- 每个字段记录真实来源，允许多个字段引用同一可靠来源。

## Excel 与工具链

- 主表固定在第一个 Sheet，标题行与表头分离，冻结窗格为 `C3`。
- GB/T 42594 未命中时只合并第 19 至 24 列，不影响新增监管列。
- 同名文件不再覆盖，自动增加 `_v2`、`_v3` 后缀。
- 新增 `requirements.txt`、监管索引构建器和 9 项回归测试。
- 加固 SDS 参考库构建脚本的 ZIP 路径识别、原子输出和索引行号生成。

## 数据规模

- SDS 本地库：1006 条记录，其中 33 条人工核验。
- 易制毒：45 条检索记录。
- 易制爆：95 条检索记录，其中 27 条需要核实浓度或形态。
- 统一监管索引：110 个 CAS 键、127 个名称键、154 个别名键。

## 验证

- Codex skill 结构校验通过。
- 监管索引重建及完整性校验通过。
- 9 项自动化测试通过。
- 工作簿保存、重新打开及 ZIP 完整性验证通过。

在已有 Git 安装的 skill 目录中升级并验证：

```bash
git pull --ff-only
pip install -r requirements.txt
python tools/build_regulated_index.py --check
python -m unittest discover -s tests -v
```
