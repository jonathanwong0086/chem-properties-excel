---
name: chem-properties-excel
description: >
  从工艺流程文本、SDS摘要、或化学品列表中提取所有化学品，联网查询物性数据，
  交叉比对中国法规标准，生成带来源注释的Excel物性数据表。
  支持脏数据（大段工艺描述）、干净列表、或半成品输入。
  Trigger: "物性表" / "化学品数据" / "chemical properties" / "生成物性excel"
version: 2.6.0
allowed-tools: [Read, Write, Edit, Bash, WebSearch, WebFetch, Glob, Grep, Agent, AskUserQuestion]
---

# 化学品物性数据Excel生成器

从任意格式的化学品输入出发，生成一份**全程可追溯、带来源注释**的Excel物性数据表。
最终产物包含四个Sheet：物性数据主表 + 介质-设备对照表（可选） + 数据来源汇总表 + 化学品清单（可选）。

---

## 0. 输入识别

用户输入可能是以下任意形态，**不要求用户预先整理**：

| 形态 | 特征 | 处理策略 |
|------|------|----------|
| **脏数据** | 大段工艺描述、SDS全文、会议记录、混杂文字 | Step 1: 提取 |
| **干净列表** | 已整理的化学品名称列表 | 跳过 Step 1 |
| **半成品** | 名称+部分属性已知，缺其他属性 | Step 1 跳过已有属性，仅补缺 |
| **设备一览表** | 包含"物料介质"/"介质"列的设备清单xlsx | Step 0.5: 从设备表提取 |

收到输入后，先判断属于哪种形态，再决定从哪一步开始。

---

## 0.5 设备一览表介质提取（当输入为设备表时）

当用户提供设备一览表（含设备位号、名称、工段、物料介质等列）时：

### 介质列识别
设备一览表通常按设备类型分sheet（如反应釜、塔、换热器、容器、泵等），每个sheet的列结构不同。**不要预设列位置**，应按以下方法动态识别：

1. 读取每个sheet的第2~3行表头行（含合并单元格）
2. 按关键字匹配介质列：搜索包含 `介质`、`物料`、`主要介质`、`主要成分` 的表头单元格
3. 同时识别位号列（含 `位号`、`设备位号`）、名称列（含 `设备名称`、`名称`）、工段列（含 `工段`、`工序`）
4. 换热器类设备通常有管程/壳程两组介质，均需提取

### 化学品名称归并

设备表中的介质写法常有**变体、错别字、浓度前缀**，需做规范化映射：

**常见归并模式：**
- 异体字：噁唑酮 = 嗯唑酮 = 唑酮；吡咯腈 = 吡咯晴
- 同义变体：丙烯腈 = 丙烯晴 = 2-氯丙烯腈 = 2-氯丙烯睛
- 浓度前缀剥离：27.5%双氧水 → 过氧化氢(双氧水)；32%液碱 → 氢氧化钠
- 少量/微量前缀剥离：少量甲苯 → 甲苯；微量氯化氢 → 氯化氢(气)
- 别名归一：氯甲醚 → 氯甲基乙醚；二氯氧基甲烷 → 二乙氧基甲烷

**公用工程/传热介质不计入工艺化学品：**
水、循环水、7℃冷水、-10℃冷冻水、蒸汽、80℃热水、导热油、乙二醇冷冻液、矿物油、白油、少量水、杂质、酸性水等。

### 工程名称标准化
为每个化学品指定一个**唯一的工程名称**（canonical name），用于后续所有sheet。

---

## 1. 化学品提取（仅脏数据需要）

从文本中识别所有化学品，包括：
- 原料、试剂、溶剂、催化剂
- 反应中间体、副产物
- 产品、目标化合物
- 辅助介质（导热油、冷媒等，标注"因牌号而异"）

**提取规则：**
- 同一物质的不同写法合并（如"盐酸"/"氯化氢水溶液"→按工艺角色分别列出）
- 气体、液体、固体均纳入
- 水、蒸汽、空气等极普通物质标注后列出，但可与用户确认是否纳入
- 提取完成后列出清单，**请用户确认或增删**后再进入下一步

**输出格式（内部）：**
```
序号 | 化学品名称 | 分类 | 涉及工段 | 出现设备数(去重) | 原表介质写法(变体归并) | 原文出处(章节+表/图题+摘录)
```

### 1.1 原文位置引用格式（重要，强制）

当输入是经 **MinerU / OCR 转换**的报告、安全评价报告、工艺文件等（正文夹带 `P12`、`T3`、`IMAGE_xxx`、`IMAGE***` 之类的页码/表/图**内部标记**）时，**绝不能把这些内部标记当作"原文位置/出处"输出给用户**——用户看不懂 `P* T* IMAGE***`，这类标记只是转换工具的锚点，不是可读来源。

凡是要向用户交代"这条信息来自原文哪里"（提取清单的出处列、化学品来源说明、Excel 单元格批注中引用用户输入文档时），**一律转换为"章节号 + 表/图题 + 原文摘录"三段式：**

```
第<章节号>节 <表/图编号《题名》>："<原文摘录>"
```

**示例：**
```
第4.5.3节表4.5.3-2《报警仪设置情况表》："105车间……三乙胺、甲苯、丙酮等"
```

**转换方法：**
1. **章节号**：从该 MinerU 标记附近的正文向上回溯，找到最近的章节标题（如 "4.5.3 报警仪设置"），取其编号 → `第4.5.3节`。
2. **表/图题**：`T*` / `IMAGE***` 标记对应的题注通常紧邻其上一行或下一行（如 "表4.5.3-2 报警仪设置情况表"），**原样取表/图编号 + 题名**（题名加《》）。正文段落无表图时此段省略。
3. **原文摘录**：截取含该化学品的关键原文短句，保留原文用词，过长时用 "……" 省略中间无关内容，但**必须保留化学品名称本身**。摘录加英文双引号。
4. **缺段降级**：三段信息任一缺失时，能取几段写几段（如无表图题则写 `第4.5.3节："……原文摘录……"`；连章节号都无法定位时写 `原文摘录："……"`）。
5. **硬性禁止**：转换后的输出中**一律不得再出现 `P<数字>`、`T<数字>`、`IMAGE_xxx`、`IMAGE***` 等任何 MinerU/OCR 内部标记**。若无法从上下文还原出人类可读位置，宁可只给原文摘录，也不得回退到内部标记。

---

## 2. 物性数据采集

**查询顺序（严格遵守）：先查本地《全书》库，未命中/字段缺失再联网。**

对每种化学品，**第一步**先按 §2.0 查本地 `sds-handbook-reference.jsonl`；该化学品在本地库中已记载的字段直接原样采用，**不必再联网核实**。只有本地库未命中该化学品，或命中但某些字段在 `physchem_text` 中未提及/缺失时，才对**缺失的字段**通过 WebSearch + WebFetch 联网查询。

以下14个字段均适用本规则（毒理数据字段见 §3.6，走同一本地库但索引位置不同）：

| 字段 | 英文 | 单位 | 首选来源（本地未命中时） |
|------|------|------|----------|
| CAS号 | CAS Number | — | PubChem |
| 熔点 | Melting Point | °C | NIST WebBook |
| 沸点 | Boiling Point | °C | NIST WebBook / ChemicalBook |
| 闪点 | Flash Point | °C | Sigma-Aldrich SDS |
| 密度 | Density | g/cm³ 或 g/L | PubChem |
| 常温物态 | State at RT | 固/液/气 | ChemicalBook / SDS |
| 火灾危险性类别 | Fire Hazard Class | 甲/乙/丙/非可燃 | GB 50016-2014 |
| 水溶性 | Water Solubility | 描述 | PubChem |
| 密度>水？ | Density > Water | 是/否 | 计算 |
| 禁用水灭火？ | Water Extinguishing Prohibited | 是/否 | 本地`firefighting_text`（见§2.0第3点）/ CAMEO / SDS |
| 爆炸极限 | Explosion Limits | LEL%~UEL% | NIOSH Pocket Guide |
| 蒸汽>空气？ | Vapor > Air | 是/否(MR) | NIST WebBook |
| 职业接触限值 | OEL | mg/m³ (TWA/STEL/MAC) | GBZ 2.1-2019 |
| 危化品类别 | Danger Category | 易燃液体/腐蚀品等 | 危险化学品目录(2015版) |

### 2.0 本地《危险化学品安全技术全书》优先查询

**优先读取本地参考文件：** `sds-handbook-reference.jsonl` + `sds-handbook-index.json`（位于本skill同目录）。**这是本节所有理化性质字段的第一查询来源，联网检索是补充手段，不是默认手段。**

**查找方法：**
1. 按 `sds-handbook-index.json` 的 `lookup_order`（`cas_no` → `name_zh` → `aliases_zh` → `name_en` → `aliases_en`）用CAS号/中文名/别名/英文名匹配，命中后得到 `sds-handbook-reference.jsonl` 中的行号（0-based，逐行对应一条记录）。命中多行时优先取 `cas_no` 精确匹配且 `verified=true` 的记录。
2. 取该记录的 `physchem_text`（理化特性原文），从中**逐字段提取**表中14个字段里能确定的部分（如熔点、沸点、闪点、密度、外观/物态、水溶性等——原文怎么写就怎么摘，不得意译或换算单位造成失真，除非原文本身给出换算所需的两个量）。
3. **禁用水灭火？** 单独取该记录的 `firefighting_text`（消防措施原文），根据"灭火剂用xx"是否包含水/雾状水判定：明确用水或雾状水灭火→填"否"；灭火剂列表不含水（如仅列泡沫/干粉/二氧化碳/砂土）且未提及水→ 结合原文是否出现"用水灭火无效"等直接否定表述判定为"是"；原文未给出足够信息时不得凭空判定，转入联网查询CAMEO/SDS核实。
4. 火灾危险性类别、危化品类别、职业接触限值这三项仍按 §3.1/§3.2/§3.3 对应的法规目录判定，`physchem_text`/`firefighting_text` 只作为物理性质/消防数据来源，不替代法规分类判定。
5. `physchem_text`/`firefighting_text` 中未提及的字段（如未写密度，或消防措施未提供足够信息判定禁用水灭火），视为该字段本地未命中，转入联网查询（§2.5）。
6. **来源批注：** 命中本地库的字段批注写 `数据来源:\n《危险化学品安全技术全书·通用卷》(第三版) 第<source_page>页，条目 <entry_id>`；`verified=false` 的记录（本书1006条中973条属此类，为OCR+规则自动识别）批注末尾追加 `⚠ 该条目未经人工核实(OCR自动识别)，如需用于正式合规文件请核对原书或联网复核`。

**数据采集原则：**
- 每个字段的来源批注必须真实反映实际查询路径（本地库 vs 具体联网来源），不得篡改或省略来源
- 中国法规数值优先（火灾危险性、OEL、危化品类别）——这三项走法规目录，不受本地《全书》库结果影响
- 不可燃物质的闪点/爆炸极限标注"—"
- 未知或因牌号而异的标注"因牌号而异"或"数据未公开"
- **工艺专有中间体（无公开CAS号）：物理常数标"数据未公开"，火灾类别标"丙类(估)"或"待定"，来源注明"工艺专有中间体，无公开数据，按有机物估计"——绝不编造数值**

### 并行Agent批次查询策略

将化学品分为4~6批，每批用独立Agent并行查询。**Agent必须将结果写入JSON文件**（而非直接返回文本，避免上下文截断）：

```
批次1(溶剂/通用): Agent写入 → _props_b1.json
批次2(酸/试剂):   Agent写入 → _props_b2.json
批次3(胺/气体):   Agent写入 → _props_b3.json
批次4(中间体):    Agent写入 → _props_b4.json
```

**Agent Prompt关键指令：**
- 明确列出每种化学品的中文名（JSON key），要求Agent原样使用
- 要求Agent用Write工具写入指定路径的UTF-8 JSON文件
- 每种化学品15个字段（CAS~危化品类别 + 来源），全部字符串值
- 用"—"标记不适用项，"数据未公开"标记查不到的数据
- Agent完成后返回"DONE bN"确认

**合并后验证：** 检查每个JSON文件是否可解析、条目数是否正确、是否覆盖所有化学品。

---

## 2.5 数据源完整URL与查询指南

### 国际数据库

| 来源 | 完整URL | 查询方法 |
|------|---------|----------|
| **PubChem** | `https://pubchem.ncbi.nlm.nih.gov/compound/<CAS号>` | 用CAS号搜；页面Sections: Chemical & Physical Properties, Solubility, Safety & Hazards |
| **NIST Chemistry WebBook** | `https://webbook.nist.gov/cgi/cbook.cgi?ID=<CAS号>&Units=SI` | 直接用CAS号查，页面包含：Phase change data (熔沸点), Gas phase thermochemistry (蒸气密度) |
| **ChemSpider** | `http://www.chemspider.com/Search.aspx?q=<CAS号>` | 搜索CAS号，Properties部分含物态、密度 |
| **ChemicalBook** | `https://www.chemicalbook.com/ProductChemicalProperties_CB<编号>_EN.htm` | 搜索CAS号或中文名，首页即显示熔点/沸点/闪点/密度/物态表 |
| **Sigma-Aldrich SDS** | `https://www.sigmaaldrich.com/` → 搜索产品 → Download SDS | SDS中Section 9: Physical & Chemical Properties (闪点/密度/物态) |
| **CAMEO Chemicals** | `https://cameochemicals.noaa.gov/chemical/<CAS号>` | Reactivity: 遇水反应性；Flammability: 爆炸极限；Health: 禁用水？ |
| **NIOSH Pocket Guide** | `https://www.cdc.gov/niosh/npg/npgd<编号>.html` | Explosive limits (LEL/UEL)；IDLH值；Relative Density (蒸气密度MR) |
| **FAO/WHO JMPR** | `https://www.fao.org/agriculture/crops/` | 农药原药物化及毒理数据 |

### 中国法规标准

| 来源 | 查询网址 | 查询方法 |
|------|----------|----------|
| **GB 50016-2014** 《建筑设计防火规范》 | `https://openstd.samr.gov.cn/` | **表3.1.1**：火灾危险性分类——按闪点划分甲乙丙类 |
| **GBZ 2.1-2019** 《工作场所有害因素职业接触限值》 | `https://openstd.samr.gov.cn/` | **表1**：OEL值——TWA/STEL/MAC |
| **GB 15603-1995** 《常用危险化学品贮存通则》 | `https://openstd.samr.gov.cn/` | 附录：遇湿易燃物质、禁止水灭火物质清单 |
| **GB/T 42594-2023** 《承压设备介质危害分类导则》 | `https://openstd.samr.gov.cn/` | 附录A：毒性分级——I级/II级气体蒸气 |
| **GB/T 50493-2019** 《石油化工可燃气体和有毒气体检测报警设计标准》 | `https://openstd.samr.gov.cn/` | **附录B**：常见有毒气体、蒸气特性表 |
| **HG/T 20660-2017** 《压力容器中化学介质毒性危害和爆炸危险程度分类标准》 | `https://openstd.samr.gov.cn/` | **附录A**：毒物危害指数I/II级物质（气体/蒸气） |
| **《高毒物品目录》** (卫法监发[2003]142号) | `https://www.mem.gov.cn/` | 54种高毒物质MAC/TWA/STEL |
| **《危险化学品目录》(2015版)** | `https://www.mem.gov.cn/` | 是否列入；备注栏标注"剧毒" |

### 查询优先级

```
1. 中国法规有明确数值 → 优先采用（OEL、火灾类别、危化品类别）
2. 国际数据库有数据 → 采用（物化属性：熔点、沸点、密度等）
3. 供应商SDS有数据 → 参考（闪点、遇水反应性等）
4. 以上均无 → 标注"数据未公开"或"因牌号而异"
```

### 查询关键词模板

**优先级（严格遵守，不得跳过第1轮就断定"数据未公开"）：**

1. **中文名 + CAS号精确组合**（首选，命中率最高）：`"<中文名> <CAS号> 分子式 外观"`、`"<中文名> CAS <CAS号> 熔点 沸点 闪点 密度"` — 即使CAS号来源尚未确认也应先试，这类查询能稳定命中 ChemicalBook 中文站、Capotchem（capotchem.com）、化源网（chemsrc.com）等中文化工数据库的目标物质页面。
2. 若第1轮中文查询无结果，再试英文名/纯模糊描述作为补充，**不作为首选**：`"<英文名> boiling point melting point flash point density SDS"`。
3. 第1、2轮均无结果时才可标注"数据未公开"——两轮都试过才算查过，只试英文或只试模糊短语不算。

```
# 物化属性（中文名+CAS号优先）
"<中文名> <CAS号> 分子式 外观"
"<中文名> CAS <CAS号> 熔点 沸点 闪点 密度"
"<英文名> boiling point melting point flash point density SDS"   # 第1轮无果时补充

# 安全数据
"<中文名> 安全技术说明书 SDS 爆炸极限"
"<英文名> NIOSH pocket guide explosion limits"

# 中国法规
"GBZ 2.1-2019 <化学名> 职业接触限值"
"危险化学品目录 <化学名> CAS <CAS号>"
```

**注意事项：**
- 百度百科/百度系站点本身很难通过 WebSearch 直接命中（即使显式加"百度百科"关键词），排名普遍靠后——但这只是百度一个站点的问题，不代表中文化工数据源整体查不到。不要把"百度查不到"等同于"中文数据源查不到"。
- **手性体/消旋体命名陷阱：** L型/D型/DL消旋体的CAS号不同，供应商站点通常只收录他们卖的那一个构型。查到CAS后必须核对该构型是否与用户给定的名称/需求一致（如"邻氯苯甘氨酸"若为L型，CAS与DL消旋体不同），避免张冠李戴。

---

## 3. 法规分类判定

### 3.1 火灾危险性（GB 50016-2014 表3.1.1）

> ⚠ **重要限制：本skill的火灾危险性类别判定仅依据闪点（及气体爆炸下限）这一项指标，不是GB 50016-2014表3.1.1的完整判据。** 该表原文对甲/乙/丙类液体还有其他并列条件（如爆炸下限、闭杯/开杯闪点区分、特定气体判据等），本skill未逐一核对，仅取"闪点"这一最常用判据做简化分类。**结果仅供参考，正式消防设计/合规判定必须以当地消防部门核定或专业人员依据完整表3.1.1条文为准，不能直接采信本表数据。**

| 分类 | 判定依据 | 颜色 |
|------|----------|------|
| **甲类** | 闪点 < 28°C 的液体；爆炸下限 < 10% 的气体 | 红色 FFCCCC |
| **乙类** | 28°C ≤ 闪点 < 60°C 的液体 | 橙色 FFE0B2 |
| **丙类** | 闪点 ≥ 60°C 的液体；可燃固体 | 黄色 FFF9C4 |
| **非可燃** | 不可燃液体/固体/气体（闪点不适用） | 白色 FFFFFF |

**仅甲/乙/丙着色，其余统一标"非可燃"白色底。**

**特殊物质注意：**
- 遇水反应品（如PCl₃）：不可燃，不归入甲乙丙，标"非可燃"
- 氧化剂（如H₂O₂、Br₂）：不可燃，标"非可燃"
- 强酸（如H₂SO₄、HCl）：不可燃，标"非可燃"

### 3.2 职业接触限值（GBZ 2.1-2019）

**优先读取本地参考文件：** `gbz2.1-oel-reference.md`（位于本skill同目录），包含约300种化学品的CAS号+MAC/TWA/STEL。按CAS号比对即可，无需联网。

> 注意：本地参考数据基于GBZ 2.1-2007版提取，2019版为现行版本。若化学品未在本地文件中找到，应联网查询GBZ 2.1-2019确认。

- MAC（最高容许浓度）：急性毒性物质
- TWA（时间加权平均）：8h工作日平均
- STEL（短时间接触）：15min短时间
- 单位统一为 mg/m³

### 3.3 危险化学品目录（2015版）

**优先读取本地参考文件：** `hazcat-2015-reference.md`（位于本skill同目录），包含2674种化学品的CAS号+危险性类别+备注（含"剧毒"标记）。按CAS号比对即可，无需联网。

**填写规则（严格遵守）：**
- **在目录中：** 必须原样誊写目录中的"危险性类别"文本（如"易燃液体,类别2"、"急性毒性-经口,类别3\*"、"皮肤腐蚀/刺激,类别1A"等），不得简化或改写。同一CAS在目录中有多行条目时，合并所有条目的危险性类别，用"；"分隔。
- **不在目录中：** 填写 `未录入《危险化学品目录》，需查阅SDS确认GHS分类`。**禁止编造GHS分类数据**——如果无法通过可靠来源（PubChem、ECHA C&L Inventory、供应商SDS原文）实际查到该化学品的GHS分类，就只写"未录入"，不得拼凑或猜测分类结果。批注中注明"CAS xxx 未在《危险化学品目录》(2015版)中查到，需查阅该化学品的SDS或ECHA C&L Inventory确认GHS危险性分类"。
- **禁止使用笼统描述：** 不得写"易燃液体"、"腐蚀品"、"有毒品"等笼统词，必须用目录原文的GHS分类格式（含类别编号）。
- **必须使用中文：** 所有危害类别文本必须用中文书写（如"易燃液体,类别2"），禁止出现英文（如"Flammable liquids, Cat 2"）。联网查询到的英文GHS分类也必须翻译为中文后填写。

### 3.4 有毒气体检测目录

对照以下四个来源，判定每种化学品是否列入有毒气体检测目录。

**优先读取本地参考文件：** `toxic-gas-directory-reference.md`（位于本skill同目录），包含完整的高毒物品目录54种、GB/T 50493-2019附录B 3种、HG/T 20660-2017附录A 66种、剧毒物质148种CAS号。直接按CAS号比对，无需联网。

| 来源 | 性质 | 文案 |
|------|------|------|
| 《高毒物品目录》(卫法监发[2003]142号) | 54种高毒物质 | **是，应设报警，来自《高毒物品目录》** |
| GB/T 50493-2019 附录B | 常见有毒气体蒸气特性表 | **是，暂不设报警，来自GB/T 50493-2019** |
| HG/T 20660-2017 附录A | 毒物危害指数I/II级（剔除剧毒后66种，标黄） | **是，暂不设报警，来自HG/T 20660-2017** |
| 《危险化学品目录》(2015版)备注"剧毒" | 剧毒物质 | 与高毒物品目录合并表述 |

**文案规则（严格遵守）：**
- 命中高毒物品目录或危化品目录剧毒：`是，应设报警，来自《高毒物品目录》`
- 命中GB/T 50493-2019附录B或HG/T 20660-2017附录A（标黄物质）：`是，暂不设报警，来自GB/T 50493-2019` 或 `是，暂不设报警，来自HG/T 20660-2017`
- 未命中任何来源：`否`

**着色：** "暂不设报警"的行用黄底（FFF9C4），其余不着色。

**来源注释：** 每个单元格批注中写明具体出处，如"HG/T 20660-2017附录A"或"《高毒物品目录》(卫法监发[2003]142号)"。

### 3.5 GB/T 42594-2023 介质危害分类（6个补充字段）

**优先读取本地参考文件：** `gbt42594-2023-reference.json`（位于本skill同目录），为GB/T 42594-2023《承压设备介质危害分类导则》的结构化附录。**无需联网。**

**查找方法（按search_index，逐个字段原样抄录）：**

1. 用 `search_index.by_cas[<CAS号>]` 定位记录（返回 `{table, row, name}`）；查不到CAS时依次尝试 `by_un[<UN号>]`、`by_name[<中文名>]`（by_name 同时含别名）。
2. 命中后读取 `tables[A.1 或 A.2].records[row-1]`，取以下6列，**原样抄录，不做任何加工**：

| 输出列 | JSON字段key | 说明 |
|--------|------------|------|
| 毒性(GB/T42594) | `毒性a,b` | 格式多样（`IV`、`III *`、`IV / III`、`III/II`、`-(II-受限区)/I`、`III*/II`等）——**罗马数字、斜杠、星号、括号全部原样抄过来，不需要处理、不做归一、不做解释** |
| 燃烧爆炸特性 | `燃烧爆炸特性` | 原样抄录 |
| 介质与金属材料相容性提示 | `介质与金属材料相容性提示d` | 原样抄录 |
| 反应和稳定性提示 | `反应和稳定性提示` | 原样抄录 |
| 危害提示 | `危害提示` | 原样抄录 |
| GHS健康和环境危害类别 | `GHS健康和环境危害类别b` | 原样抄录 |

3. 空值（源为 `—`/`-`/空）原样保留。查不到该CAS/UN/名称时，6列均填 `未列入GB/T 42594-2023`。

**来源批注：** 6列每个单元格批注统一写 `数据来源:\nGB/T 42594-2023《承压设备介质危害分类导则》附录A（表A.1/A.2），CAS <CAS号>`。

**表注（放入说明区域，改写"该列"表述，避免脱离上下文歧义）：**
```
关于GB/T 42594-2023 毒性列的说明（原标准表A脚注a/b/c，此处将"该列"改指"毒性(GB/T42594)列"）：
a. 毒性(GB/T42594)列中，"/"左侧的介质毒性危害程度等级是根据急性毒性的分类结果；"/"右侧的介质毒性危害程度等级是按照GBZ 230进行分类的结果，适用于在室内相对密闭的环境中使用的承压设备和事故状态等异常工况。若未分别列出，则表示根据急性毒性和GBZ 230获得的介质毒性危害程度等级相同。毒性(GB/T42594)列中，"/"左侧括号内毒性危害等级是在确定设备的密封性、致密性等技术要求时，考虑致癌性后介质的毒性危害程度等级。
b. 标记 * 的类别，在有充分依据的条件下，或综合考虑最高允许浓度及多种危害等情况，该介质可以采用更严格的类别。其他标准、规范有更严格要求时，可从其规定。
c. 其他危害包括自反应介质、自燃介质、遇水放出易燃气体的介质、氧化性介质、有机过氧化物、金属腐蚀性介质等危害分类。
```

### 3.6 毒理数据（LD50/LC50等，来自《危险化学品安全技术全书》）

**优先读取本地参考文件：** `sds-handbook-reference.jsonl` + `sds-handbook-index.json`（位于本skill同目录），数据来自《危险化学品安全技术全书·通用卷》(第三版) SDS章节的结构化提取（经作者授权转载）。**未命中或字段缺失时再联网查证补充，不得跳过本地查询直接联网。**

**查找方法：**

1. 读取 `sds-handbook-index.json` 的 `index`，按 `lookup_order`（`cas_no` → `name_zh` → `aliases_zh` → `name_en` → `aliases_en`）依次用CAS号/中文名/别名/英文名匹配，命中后得到一个或多个行号（0-based）。
2. 按行号在 `sds-handbook-reference.jsonl` 中取出对应行（第N行 = 第N个JSON对象，从0开始数），解析该行JSON。命中多行时，优先选 `cas_no` 命中且 `verified=true` 的记录；都不满足时取第一个命中项，若多条记录内容冲突需在批注中说明。
3. 取该记录的 `toxicology_text`（毒理学信息原文）与 `physchem_text`（理化特性原文，供第2节物性数据核对/补充用）。
4. **毒理数据列填写：** 将 `toxicology_text` 中的急性毒性数据（LD50、LC50，含物种、给药途径、数值单位）**原样摘录**到"毒理数据(LD50/LC50)"列，保留原文表述，不拆分成结构化子字段（不同条目的物种/给药方式/单位差异很大，拆分易出错）。若 `toxicology_text` 过长，可截取其中LD50/LC50相关语句，其余（如刺激性、致敏性等）按需精简，但不得篡改数值。
5. **本地未命中：** 联网查询（PubChem "Toxicity" 章节、NIOSH Pocket Guide、供应商SDS第11节"毒理学信息"），查到则照常填写并注明联网来源；查不到则填 `未查到毒理数据`。

**可信度标注（必须遵守）：** 记录的 `verified` 字段为 `false`（本书1006条中有973条属此类，为OCR+规则识别，未经人工核对数据库核实）时，该条毒理数据单元格批注末尾必须追加一行 `⚠ 该条目未经人工核实(OCR自动识别)，如需用于正式合规文件请核对原书或联网复核`；`verified=true`（仅33条）时批注正常注明来源即可，无需该警示。

**来源批注格式：**
- 命中本地库：`数据来源:\n《危险化学品安全技术全书·通用卷》(第三版) 第<source_page>页，条目 <entry_id>`（+ 上述可信度警示，如适用）
- 联网补充：按第2节的联网来源批注规则（注明具体URL/数据库名）

---

## 4. Excel 生成

使用 Python + openpyxl 生成，**必须包含以下内容**：

### Sheet 1：物性数据主表

**列结构（24列，含毒理数据列 + 有毒气体列 + GB/T42594 6列）：**
```
序号 | 化学品名称 | CAS号 | 熔点(°C) | 沸点(°C) | 闪点(°C) | 密度 | 常温物态 | 火灾危险性类别 | 水溶性 | 密度>水？ | 禁用水灭火？ | 爆炸极限(LEL~UEL) | 蒸汽>空气？ | 职业接触限值(mg/m³) | 危化品类别 | 毒理数据(LD50/LC50) | 是否被列入有毒气体检测目录 | 毒性(GB/T42594) | 燃烧爆炸特性(GB/T42594) | 介质与金属材料相容性提示(GB/T42594) | 反应和稳定性提示(GB/T42594) | 危害提示(GB/T42594) | GHS健康和环境危害类别(GB/T42594)
```

**毒理数据列填写：** 严格按 §3.6，优先从本地 `sds-handbook-reference.jsonl`/`sds-handbook-index.json` 查得后摘录 `toxicology_text` 中的LD50/LC50语句；本地未命中再联网查证。未经人工核实的条目须在批注中追加可信度警示。

**GB/T42594 6列填写：** 严格按 §3.5，从本地 `gbt42594-2023-reference.json` 按CAS/UN/中文名/别名查得后**原样抄录**，不加工。

**格式要求：**
- 表头：深蓝底白字，微软雅黑 10pt 加粗
- 数据行：微软雅黑 9pt，自动换行
- 统一行高：30px
- 列宽紧凑但不遮盖：5~26字符
- **冻结窗格 `C3`**：同时冻结标题行+表头行（前2行）与序号列、化学品名称列（前2列）。横向滚动查看后段列时序号和物质名称始终可见。

**未列入GB/T42594时后6列合并：**
- 当某物质在 `gbt42594-2023-reference.json` 中按 CAS/UN/中文名/别名均查不到时，**将该行第19~24列（毒性→GHS）6个单元格横向合并为一个**，只写一次 `未列入GB/T 42594-2023`，居中。
- 命中的物质仍按6列分别填写，不合并。
- 实现：`ws.merge_cells(start_row=r, start_column=19, end_row=r, end_column=24)`，合并前只给左上单元格写值与批注。

**颜色标注：**

*（1）火灾类别三列高亮*
- 化学品名称列、CAS号列、火灾危险性类别列
- 甲类=红底 FFCCCC / 乙类=橙底 FFE0B2 / 丙类=黄底 FFF9C4 / 非可燃=白底

*（2）斑马行底色（其余列，避免长表看串行）*
- 对**未被火灾类别高亮**的单元格，按数据行序号循环施加 4 档淡色，兼顾黑白打印（浅到几乎无墨，不浪费碳粉）：

  | 循环位 | 色号 | 说明 |
  |--------|------|------|
  | 第1行 | FFFFFF | 白 |
  | 第2行 | F7FAFD | 淡淡淡蓝 |
  | 第3行 | EEF4FA | 淡淡蓝 |
  | 第4行 | E4EEF7 | 淡蓝 |

- 斑马色**不得覆盖**甲/乙/丙高亮的名称/CAS/火灾类别三列——这三列的高亮优先。
- 4 档循环（每 4 行一组）比两档更易在密集长表中定位行，且最深档仍属"淡蓝"，打印墨量低。

**来源注释（关键）：**
- 每个数据单元格添加 Excel Comment
- 同一行不同列的注释来源**必须不同**
- 格式：`数据来源:\n<机构名> (<完整URL>)`
- 注释宽350、高70
- **引用用户输入文档（报告/安评/工艺文件）作为来源时，位置一律按 §1.1 写成"章节号 + 表/图题 + 原文摘录"三段式，禁止出现 `P*`/`T*`/`IMAGE***` 等 MinerU/OCR 内部标记。**

**说明区域（数据行下方）：**
```
说明:
1. 每个单元格右上角红三角 = 来源注释，同一行不同列来源不同。
2. 【重要】火灾危险性类别仅依据闪点(及气体爆炸下限)简化判定，非GB 50016-2014表3.1.1完整判据，仅供参考，正式消防设计/合规判定须以当地消防部门核定为准。
3. 仅标注甲/乙/丙类，非可燃物质统一标注为"非可燃"。
4. 职业接触限值依据 GBZ 2.1-2019，单位 mg/m³。
5. 危化品类别依据《危险化学品目录》(2015版)。
6. 因牌号/结构未定的物质，标注"因牌号而异"，需查阅实际供应商SDS。
7. 有毒气体检测目录依据《高毒物品目录》、GB/T 50493-2019附录B、HG/T 20660-2017附录A。
8. 毒性/燃烧爆炸特性/介质与金属材料相容性提示/反应和稳定性提示/危害提示/GHS健康和环境危害类别 6列均来自GB/T 42594-2023《承压设备介质危害分类导则》附录A，按CAS/UN/名称查得后原样抄录；未列入者该6列合并，只标注一次"未列入GB/T 42594-2023"。
9. 毒理数据(LD50/LC50)列及其余理化性质字段优先来自本地《危险化学品安全技术全书》，未经人工核实的条目(标注⚠)如需用于正式合规文件请核对原书或联网复核。
```

（毒性列脚注 a/b/c 见 §3.5 表注，须一并写入说明区域。）

### Sheet 2：数据来源汇总

**列结构（4列）：**
```
类型 | 机构/网站名称 | 本次提供数据字段 | 网址（带超链接）
```

**必含来源（16项）：**

| 类型 | 来源 | 提供字段 |
|------|------|----------|
| 国际数据库 | PubChem (NCBI) | CAS号、密度、水溶性 |
| 国际数据库 | NIST Chemistry WebBook | 熔点、沸点、蒸气相对密度 |
| 化学品供应商 | Sigma-Aldrich / MilliporeSigma | SDS、闪点、密度 |
| 化学品数据库 | ChemicalBook | CAS号、沸点、闪点、物态 |
| 化学品数据库 | ChemSpider (RSC) | 常温物态 |
| 应急响应数据库 | CAMEO Chemicals (NOAA) | 遇水反应性、灭火禁忌 |
| 职业安全数据库 | NIOSH Pocket Guide (CDC) | 爆炸极限、蒸气密度 |
| 农药评价 | FAO/WHO JMPR、BASF SDS | 农药原药物化及毒性 |
| 中国国家标准 | GB 50016-2014 | 火灾危险性类别 |
| 中国国家标准 | GBZ 2.1-2019 | 职业接触限值 |
| 中国国家标准 | GB 15603-1995 | 储存禁忌 |
| 中国国家标准 | GB/T 42594-2023 | 毒性、燃烧爆炸特性、介质与金属材料相容性提示、反应和稳定性提示、危害提示、GHS健康和环境危害类别 |
| 中国国家标准 | GB/T 50493-2019附录B | 有毒气体蒸气特性表 |
| 中国行业标准 | HG/T 20660-2017附录A | 毒物危害分类I/II级 |
| 中国部门规章 | 《高毒物品目录》(卫法监发[2003]142号) | 高毒物质OEL |
| 中国部门规章 | 《危险化学品目录》(2015版) | 危化品类别、剧毒 |

**网址列使用超链接样式（蓝色下划线），可点击跳转。**

### Sheet 3：化学品清单（可选，当从设备表提取时）

**列结构：**
```
序号 | 化学品名称 | 分类 | 涉及工段 | 出现设备数(去重) | 原表介质写法(变体归并) | 备注 | 是否被列入有毒气体检测目录
```

分类按：起始原料 > 溶剂 > 反应原料 > 反应试剂 > 调碱/中和 > 干燥/吸收 > 尾气处理 > 反应中间体 > 副产物 > 副产(盐) > 回收副产 > 反应副产(气) > 惰性保护气 > 产品。

### Sheet 4：介质-设备对照表（可选，当从设备表提取时）

**列结构：**
```
序号 | 化学品名称 | 分类 | 设备位号 | 设备名称 | 工段 | 设备类型 | 火灾危险性类别 | 是否被列入有毒气体检测目录
```

同一化学品的多台设备合并前3列单元格，交替底色分组（蓝底 #F2F7FC）。

---

## 5. 文件输出

**文件名规则：**
```
<项目名或工艺名>化学品物性数据.xlsx
```

**保存位置：** 用户桌面（`C:\Users\<username>\Desktop\`）或用户指定路径。

**编码问题处理（Windows）：**
- 中文文件名在 openpyxl 写入时可能损坏
- **解决方案：先保存为纯英文临时文件名，再 os.rename 为中文名**
- 保存后立即用 zipfile.ZipFile 校验文件完整性

```python
# 安全写入模式
tmp = os.path.join(output_dir, "chem_tmp.xlsx")
wb.save(tmp)
import zipfile
zipfile.ZipFile(tmp).close()  # 校验
os.rename(tmp, final_path)
```

**脚本清理：** 生成完毕后删除临时 Python 脚本。

---

## 6. 异常处理

| 场景 | 处理 |
|------|------|
| 某化学品CAS查不到 | 标注"数据未公开"，不中断 |
| 某属性无数据 | 标注"—"或"数据未公开" |
| 导热油等通用名 | 标注"因牌号而异"，提示查供应商SDS |
| 用户仅需部分字段 | 按用户要求裁剪列 |
| 文件被Excel锁定 | 换文件名（加_v2/_v3后缀） |
| 数据来源不确定 | 标注"计算值"或"供应商SDS" |
| 工艺专有中间体 | 物理常数"数据未公开"，火灾类别"丙类(估)"，来源注明"工艺专有，按有机物估计" |
| Agent查询中断（JSON Parse error） | Agent将结果写入文件而非返回文本；用SendMessage继续中断的Agent |
| 化学品名称变体/错别字 | 建立NORM映射表统一归并，保留原写法作为"变体"列 |
| 原文出现MinerU/OCR内部标记(P*/T*/IMAGE***) | 按 §1.1 转为"章节号+表/图题+原文摘录"，禁止把内部标记输出给用户 |
| 同一物质出现在多个设备/工段 | 合并去重，统计"出现设备数(去重)"和"涉及工段" |

---

## 7. 完整执行流程图

```
用户输入
  │
  ▼
┌──────────────────┐
│ 0.5 设备表介质提取 │ ── 按sheet类型定位介质列
│   (如适用)        │ ── 提取/归并/去重化学品
│                   │ ── 建立化学品-设备映射
└──────┬───────────┘
       ▼
┌─────────────┐
│ 1. 输入识别  │ ── 脏数据 → 提取化学品 ──→ 用户确认
│              │ ── 干净列表 → 直接进入
│              │ ── 半成品 → 补缺
└──────┬──────┘
       ▼
┌─────────────┐
│ 2. 物性采集  │ ── 并行Agent批次查询(结果写入JSON文件)
│              │ ── 每个字段不同来源
│              │ ── 合并4个批次JSON
└──────┬──────┘
       ▼
┌─────────────┐
│ 3. 法规分类  │ ── 火灾危险性(GB 50016)
│              │ ── OEL(GBZ 2.1)
│              │ ── 危化品类别(目录2015)
│              │ ── GB/T42594 6列(本地JSON按CAS/UN/名查,原样抄)
│              │ ── 有毒气体检测目录(高毒目录+GB/T50493+HG/T20660)
└──────┬──────┘
       ▼
┌─────────────┐
│ 4. Excel生成 │ ── Sheet1: 物性数据+注释+有毒气体列
│              │ ── Sheet2: 来源汇总(16项)
│              │ ── Sheet3: 化学品清单(可选)
│              │ ── Sheet4: 介质-设备对照表(可选)
│              │ ── 安全写入+校验
└──────┬──────┘
       ▼
┌─────────────┐
│ 5. 交付      │ ── 保存+zipfile校验+清理临时文件
└─────────────┘
```
